import { ColStyleDirective } from './col-style.directive';

describe('ColStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ColStyleDirective();
    expect(directive).toBeTruthy();
  });
});
