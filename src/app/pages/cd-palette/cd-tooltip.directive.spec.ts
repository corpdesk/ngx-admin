import { CdTooltipDirective } from './cd-tooltip.directive';

describe('CdTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new CdTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
